var swiper = new Swiper(".swiper", {
    slidesPerView: "auto",  // Show multiple slides at once
    spaceBetween: 20,       // Space between slides
    centeredSlides: true,   // Keep center slide in focus
    loop: true,             // Enable infinite looping
    speed: 500,             // Adjust for a smoother scroll
    freeMode: true,         // Enable smooth manual scrolling
    freeModeMomentum: true, // Enable momentum for better experience
    autoplay: false,        // Disable auto-scrolling
    allowTouchMove: true,   // Enable manual dragging/swiping
  });
  